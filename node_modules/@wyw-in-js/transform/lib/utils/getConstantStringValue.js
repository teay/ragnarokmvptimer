"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getConstantStringValue = getConstantStringValue;
var t = _interopRequireWildcard(require("@babel/types"));
var _getMemberExpressionPropertyName = require("./getMemberExpressionPropertyName");
var _unwrapExpression = require("./unwrapExpression");
function _interopRequireWildcard(e, t) { if ("function" == typeof WeakMap) var r = new WeakMap(), n = new WeakMap(); return (_interopRequireWildcard = function (e, t) { if (!t && e && e.__esModule) return e; var o, i, f = { __proto__: null, default: e }; if (null === e || "object" != typeof e && "function" != typeof e) return f; if (o = t ? n : r) { if (o.has(e)) return o.get(e); o.set(e, f); } for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]); return f; })(e, t); }
function getConstantStringValue(node) {
  const fromExpression = value => {
    const expression = (0, _unwrapExpression.unwrapExpression)(value);
    if (t.isStringLiteral(expression)) {
      return expression.value;
    }
    if (t.isTemplateLiteral(expression)) {
      var _quasi$value$cooked;
      if (expression.expressions.length !== 0) {
        return null;
      }
      const [quasi] = expression.quasis;
      return (_quasi$value$cooked = quasi === null || quasi === void 0 ? void 0 : quasi.value.cooked) !== null && _quasi$value$cooked !== void 0 ? _quasi$value$cooked : null;
    }
    if (t.isBinaryExpression(expression) && expression.operator === '+') {
      if (!t.isExpression(expression.left) || !t.isExpression(expression.right)) return null;
      const left = fromExpression(expression.left);
      const right = fromExpression(expression.right);
      if (left === null || right === null) {
        return null;
      }
      return left + right;
    }
    if (t.isCallExpression(expression)) {
      const concatProperty = t.isMemberExpression(expression.callee) ? (0, _getMemberExpressionPropertyName.getMemberExpressionPropertyName)(expression.callee) : null;
      if (concatProperty !== 'concat') {
        return null;
      }
      const {
        callee
      } = expression;
      if (!t.isMemberExpression(callee) || !t.isExpression(callee.object)) {
        return null;
      }
      const base = fromExpression(callee.object);
      if (base === null) {
        return null;
      }
      const parts = [base];
      for (const arg of expression.arguments) {
        if (!t.isExpression(arg)) {
          return null;
        }
        const part = fromExpression(arg);
        if (part === null) {
          return null;
        }
        parts.push(part);
      }
      return parts.join('');
    }
    return null;
  };
  return fromExpression(node);
}
//# sourceMappingURL=getConstantStringValue.js.map